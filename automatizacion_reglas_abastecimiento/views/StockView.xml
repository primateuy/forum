<?xml version="1.0" encoding="utf-8"?>
<odoo>

     <record id="view_location_form_inherit" model="ir.ui.view">
        <field name="name">stock.location.form.inherit.automation</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='next_inventory_date']" position="after">
                <field name="automate_reordering"/>
                <!-- <field name="location_src_id"/>  -->
                <field invisible="not automate_reordering" name="default_min_qty"/>
                <field invisible="not automate_reordering" name="default_max_qty"/>
            </xpath>
        </field>
    </record>

</odoo>
