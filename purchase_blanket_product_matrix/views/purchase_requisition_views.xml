<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_requisition_form_inherit_matrix" model="ir.ui.view">
        <field name="name">purchase.requisition.form.inherit.matrix</field>
        <field name="model">purchase.requisition</field>
        <field name="inherit_id" ref="purchase_requisition.purchase_requisition_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_open_variant_matrix"
                        type="object"
                        string="Agregar variantes"
                        class="oe_highlight"
                        attrs="{'invisible': [('state', '!=', 'draft')]}"/>
            </xpath>

            <xpath expr="//field[@name='line_ids']" position="after">
                <group col="4" class="mt16">
                    <field name="total_qty" readonly="1"/>
                    <field name="total_amount" readonly="1"/>
                    <field name="currency_id" invisible="1"/>
                </group>
            </xpath>

        </field>
    </record>
</odoo>
